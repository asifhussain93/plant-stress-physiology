[project]
name = "taskbot"
version = "0.1.0"
description = "Scaffold for an original-task software-engineering benchmark pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
